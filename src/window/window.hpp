#ifndef WINDOW_HPP
#define WINDOW_HPP

class Window {
public:

public:
  void init(void);
  int getWidth(void);
  int getHeight(void);
  Window(void);
  ~Window(void);

private:
  int width;
  int height;

private:
};

#endif
