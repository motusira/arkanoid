#ifndef WINDOW_HPP
#define WINDOW_HPP

class Window {
public:

public:
  void init(void);
  int get_widht(void);
  int get_height(void);
  Window();
  ~Window();

private:
  int width;
  int height;

private:
};

#endif
